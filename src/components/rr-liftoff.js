// rr-liftoff — structural seed stub only (Story 1.4/1.5). Not active in this story.
class RrLiftoff extends HTMLElement {
  constructor() {
    super();
    this.attachShadow({ mode: 'open' });
  }

  render() {
    // Not implemented until Story 1.4/1.5.
  }
}

customElements.define('rr-liftoff', RrLiftoff);
