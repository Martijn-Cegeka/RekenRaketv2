---
scope: RekenRaket v1 architecture spine
purpose: build-substrate
altitude: feature
updated: 2026-09-11T01:16
---

- (decision by user) Working mode: Coaching path. Deliverable: spine only, no fuller rendering.
- (decision by user) Corrected platform: not native iOS. It is a website (PWA-style), pinned to iPad home screen, so it runs anywhere a browser does. No backend. Plain HTML + Web Components (Custom Elements/Shadow DOM), no frontend frameworks/libraries. Persistence via IndexedDB. Tests required.
- (decision by user) Offline: NOT a true offline PWA. No service worker/app-shell caching required for v1 -- home-screen icon needs network on load. Resolves PRD's open offline question: network required.
- (decision by user) Paradigm: single Store module owns all app state (boxes, cards, session) and is the sole owner of IndexedDB access. Components are pure render + dispatch (CustomEvents upward), never mutate state or touch IndexedDB directly.
- (decision by user) 'Avoid libraries/frameworks' scopes to shipped app code only. Test tooling/libraries are fine (dev-only, nothing shipped to the browser).
- (decision by user) Distribution/hosting: GitHub Pages (static hosting, HTTPS by default).
- (version) Verified current (2026-09-11 web check): iOS/iPadOS 26 (latest 26.6.2) is the current major OS -- Safari on iPadOS 26 is the WebKit target. Node.js v24 is Active LTS (v26 is Current/non-LTS). Playwright 1.63 ships WebKit 26.6, matching current Safari -- good fit for e2e against the real rendering engine.
- (decision) Paradigm: Store-Mediated Web Components (lightweight Flux/unidirectional-data-flow variant, no library). Layers: domain (pure logic) -> db (IndexedDB gateway) -> store (state + orchestration) -> components (render + dispatch) -> app shell (wiring + view switching). Dependency direction: components depend on store only; store depends on domain+db; domain and db depend on nothing upstream.
- (decision) AD: domain logic (Leitner box transitions, card generation, retry cap, flight tier calc) lives in pure DOM-free ES modules under src/domain/, so node:test can exercise them without a browser.
- (decision) AD: IndexedDB schema/migrations owned by a single db.js module with explicit onupgradeneeded versioning; nothing else calls indexedDB directly.
- (decision) AD: components communicate only via CustomEvents (bubbles+composed) upward and a render(state) call downward from the Store; no direct component-to-component references, no shared globals besides the Store.
- (decision) Navigation: Store-driven view switching (currentView state field), no URL routing library or hash router -- single local session app, no deep-link requirement.
- (decision) Stack pinned: no bundler/build step -- native ES modules via <script type=module>, served as-is by GitHub Pages. Web app manifest + apple-touch-icon for home-screen name/icon (no service worker).
- (decision) Test stack: Node.js v24 LTS + built-in node:test for domain unit tests (zero deps); Playwright 1.63 restricted to the WebKit project (matches Safari/iPadOS engine) for e2e flows through real DOM + IndexedDB.
- (assumption) [ASSUMPTION] Custom element tag prefix 'rr-' and kebab-case file naming; CustomEvent names are lowercase no-dashes per DOM convention (e.g. 'cardanswered').
- (decision) Deferred: true offline/service-worker support, multi-device sync/backend (permanent non-goal per PRD), PIN-lock on settings (per PRD), CI pipeline specifics beyond GitHub Pages static hosting, detailed IndexedDB schema (owned by code once it exists).
- (event) Distilled ARCHITECTURE-SPINE.md from memlog (6 ADs, dependency-direction diagram, conventions, stack, structural seed, capability map, deferred).
- (event) Reviewer Gate run at hobby/personal stakes: scaled down per skill guidance -- ran lint_spine.py (1 low false-positive on '{operation}' template prose, no action) and an inline adversarial + version-check pass in place of full parallel subagents. Adversarial pass found one real gap: AD-4 didn't fix CustomEvent detail payload shape, allowing two components to diverge on event schema. Fixed AD-4 in place to require a flat { id, ...primitives } detail shape. No other findings.
- (event) spine finalized
- (event) Resuming for Update: reconciling against finalized UX spine pair (DESIGN.md/EXPERIENCE.md) at _bmad-output/planning-artifacts/ux-designs/ux-RekenRaketv2-2026-09-11/.
- (question) UX Component Patterns names rr-keypad, rr-run-progress, rr-settings-row alongside existing screen-level rr-fuel-tanks/rr-run-view/rr-liftoff/rr-settings -- open: are these new top-level Custom Elements or internal render fragments within their parent screen component's Shadow DOM?
- (decision) AD-7: rr-keypad, rr-run-progress, rr-settings-row render as internal Shadow DOM fragments inside their single parent screen component (rr-run-view.js / rr-settings.js), not separate top-level Custom Elements -- user chose this over giving each its own file.
- (decision) Styling convention added: rem-based text sizing (Dynamic Type per DESIGN.md), animations wrapped in prefers-reduced-motion guard (per EXPERIENCE.md Reduce Motion assumption).
- (decision) Added UX spine (DESIGN.md, EXPERIENCE.md) as companions in frontmatter.
- (event) Re-distilled ARCHITECTURE-SPINE.md from memlog (added AD-7, Styling convention row, structural-seed comments, companions).
- (event) Reviewer Gate (update, hobby stakes, scaled down): lint_spine.py -- 1 low false-positive on '{operation}' template prose (no action, same as prior run). Inline adversarial check on AD-7: rule is usage-based (current, not anticipated, reuse), no divergence found. No new stack versions introduced, nothing to re-verify.
- (event) spine finalized
